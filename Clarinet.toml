[project]
name = "void-cluster-processor"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/void-cluster-processor/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.void-cluster-processor]
path = "contracts/void-cluster-processor.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
